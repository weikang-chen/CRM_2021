<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cwk.crm.workbench.dao.ClueDao">
	
	<insert id="save">
        insert into tbl_clue(
            id,
            fullname,
            appellation,
            owner,
            company,
            job,
            email,
            phone,
            website,
            mphone,
            state,
            source,
            createBy,
            createTime,
            description,
            contactSummary,
            nextContactTime,
            address

        )
        values (
            #{id},
            #{fullname},
            #{appellation},
            #{owner},
            #{company},
            #{job},
            #{email},
            #{phone},
            #{website},
            #{mphone},
            #{state},
            #{source},
            #{createBy},
            #{createTime},
            #{description},
            #{contactSummary},
            #{nextContactTime},
            #{address}

        )
    </insert>

    <select id="getTotalByCondition" resultType="int">
        select count(*)
        from tbl_clue c
        join tbl_user u
        on u.id = c.owner
        <where>
            <if test="fullname != null and fullname != ''">
                c.fullname like '%' #{fullname} '%'
            </if>
            <if test="company != null and company != ''">
                c.company like '%' #{company} '%'
            </if>
            <if test="phone != null and phone != ''">
                c.phone like '%' #{phone} '%'
            </if>
            <if test="mphone != null and mphone != ''">
                c.mphone like '%' #{mphone} '%'
            </if>
            <if test="owner != null and owner != ''">
                u.name like '%' #{owner} '%'
            </if>
            <if test="state != null and state != ''">
                c.state like '%' #{state} '%'
            </if>


        </where>
    </select>

    <select id="getClueListByCondition" resultType="Clue">
        select
            c.id,
            c.fullname,
            u.name as owner,
            c.company   ,
            c.phone   ,
            c.mphone,
            c.state,
            c.source
        from tbl_clue c
        join tbl_user u
        on u.id = c.owner
        <where>
            <if test="fullname != null and fullname != ''">
                c.fullname like '%' #{fullname} '%'
            </if>
            <if test="company != null and company != ''">
                c.company like '%' #{company} '%'
            </if>
            <if test="phone != null and phone != ''">
                c.phone like '%' #{phone} '%'
            </if>
            <if test="mphone != null and mphone != ''">
                c.mphone like '%' #{mphone} '%'
            </if>
            <if test="owner != null and owner != ''">
                u.name like '%' #{owner} '%'
            </if>
            <if test="state != null and state != ''">
                c.state like '%' #{state} '%'
            </if>
            <if test="source != null and source != ''">
                c.source like '%' #{source} '%'
            </if>


        </where>
        order by c.createTime desc

        limit #{skipCount},#{pageSizeInt}
    </select>

    <select id="detail" resultType="Clue">
        select
            c.id,
            c.fullname,
            c.appellation,
            u.name as owner,
            c.company,
            c.job,
            c.email,
            c.phone,
            c.website,
            c.mphone,
            c.state,
            c.source,
            c.createBy,
            c.createTime,
            c.editBy,
            c.editTime,
            c.description,
            c.contactSummary,
            c.nextContactTime,
            c.address
        from tbl_clue c
        join tbl_user u
        on u.id = c.owner
        where c.id = #{id}

    </select>

    <select id="getById" resultType="Clue">
        select * from tbl_clue
    </select>


  	
</mapper>

































